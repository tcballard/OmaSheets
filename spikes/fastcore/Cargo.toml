[package]
name = "omasheets-fastcore-spike"
version = "0.0.1"
edition = "2024"
publish = false

[dependencies]
formualizer-workbook = { version = "0.8.4", default-features = false, features = ["calamine"] }
serde_json = "1.0"

[profile.release]
codegen-units = 1
lto = "thin"
strip = true
